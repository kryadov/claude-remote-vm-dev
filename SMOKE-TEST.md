# Smoke test

Created by the claude-remote-dev smoke test to verify the GitHub path end to end:
`rd-auth` -> host-scoped `gh` credential helper -> `rd-start` worktree ->
`git push` -> `gh pr create`. Safe to delete along with this branch.
